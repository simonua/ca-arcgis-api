# Fixture Boundary

Store sanitized, deterministic ArcGIS and API fixtures here. Keep fixtures small and purpose-built
for accepted, rejected, boundary, drift, timeout, and stale-data scenarios.

Tests must not fetch live ArcGIS data. Never copy editor identities, global IDs, form links,
geometry, or other fields outside the approved source contract into fixtures.
