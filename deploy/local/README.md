# Local Deployment Boundary

This directory is reserved for secret-free local examples such as `compose.example.yaml` and
`container-env.example`. Real environment files and machine-specific compose overrides are ignored
and must not be committed.

Local examples must default to fixture-backed operation with live ArcGIS access disabled.
